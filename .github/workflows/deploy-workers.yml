# Cloudflare Workers deployment removed.
# AgentDoom uses Vercel edge functions instead (decided in April 2026).
# Keeping this file to avoid confusing workflow history — it will never trigger.
# See: infrastructure/ENV_VARS.md for current infra decisions.
name: Deploy Cloudflare Workers (disabled)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'This workflow is disabled. Cloudflare Workers dropped in favor of Vercel.'
        required: true

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cloudflare Workers deployment disabled. Using Vercel edge functions."
